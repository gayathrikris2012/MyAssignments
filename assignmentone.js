const browserVersion="chrome";
function getBrowserVersion()
{
    if(browserVersion.startsWitj("chrome"))
       var browserVersion;
        console.log("Browser version is" + browserVersion);
        console.log("data type of browser version is" + typeof(browserVersion));  
     
}
getBrowserVersion();
